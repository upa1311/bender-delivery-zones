# bender-zones-v1.1 (1.1.0)

Exact-address Bender zone release (K=4, Scenario A). Zones only, no prices, no pricing schema. Working resolver uses `address-registry.json`; admin QA uses `admin-qa-objects.json`. Validate `CHECKSUMS.sha256` fail-closed before use.
